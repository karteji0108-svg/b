rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Semua upload hanya oleh user login
    match /{allPaths=**} {
      allow read: if true; // publik dapat melihat foto
      allow write: if request.auth != null;
    }
  }
}
